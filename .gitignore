node_modules/
.next/
out/
build/
.DS_Store
*.pem

# variáveis de ambiente — nunca versionar
.env
.env*.local
.env*
# ...mas o modelo sem valores deve ficar no repositório
!.env.example

npm-debug.log*
yarn-error.log*
*.tsbuildinfo
next-env.d.ts

# vínculo local com a Vercel
.vercel

# correções pontuais de dados, rodadas à mão e não versionadas
_correcao/

# documentos baixados pela busca em outra caixa — nunca versionar
documentos-encontrados/
